import { Container } from '@mantine/core';
import { Screens } from '@screens/index';
import type { FC } from 'react';

const App: FC = () => (
  <Container fluid>
    <Screens />
  </Container>
);

export { App };
